import chai from 'chai';
import { TrackEvent } from './track';

/**
 * @test {TrackEvent}
 * @todo Complete tests
 */
describe('TrackEvent', () => {
  it('should exist', () => {
    TrackEvent.should.not.be.undefined;
  });
});
