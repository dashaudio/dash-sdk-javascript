import { TrackEvent } from './track';
import { EventStatus } from '../status';

/**
 * Track Search denotes a search that was made by a user. Uses schema.org's
 * [SearchAction](https://schema.org/SearchAction) *The act of searching for an object.*.
 * @todo Revisit; not easily doable because of privacy aspect.
 */
// export class TrackSearchEvent extends Event {
//
// }
