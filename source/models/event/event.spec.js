import chai from 'chai';
import { Event } from './event';

/**
 * @test {Event}
 * @todo Complete tests
 */
describe('Event', () => {
  it('should exist', () => {
    Event.should.not.be.undefined;
  });
});
