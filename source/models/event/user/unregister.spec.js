import chai from 'chai';
import UserUnregisterEvent from './unregister';

/**
 * @test {UserUnregisterEvent}
 */
describe('User Unregister Event', () => {
  it('should exist', () => {
    UserUnregisterEvent.should.not.be.undefined;
  });
});
